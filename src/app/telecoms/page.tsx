import type { Metadata } from "next"; import { ServicePage } from "@/components/corporate/CorporateSite"; import { corporateDivisions, serviceDetails } from "@/lib/corporate/websiteContent"; export const metadata: Metadata = { title: "Telecommunications Services", description: "Fibre installation support, fibre splicing, civil works support, network infrastructure support, site coordination, and field maintenance support." }; export default function Page() { return <ServicePage division={corporateDivisions[2]} detail={serviceDetails.telecoms} />; }