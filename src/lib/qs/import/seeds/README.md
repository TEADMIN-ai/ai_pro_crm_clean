# QS Import Seed Datasets

These CSV files are import-ready reference datasets for the Torque Empire QS Engine.

They are intentionally not auto-seeded into production Firestore. Use them as controlled import inputs for development, QA, and future admin import tooling.
