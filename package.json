{
  "name": "ai_pro_crm",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",

    "clean": "rimraf .next",
    "typecheck": "tsc --noEmit",
    "lint": "next lint",

    "build:strict": "npm run clean && npm run typecheck && npm run lint && next build",
    "deploy:guard": "npm run build:strict"
  },

  "dependencies": {
    "firebase": "^10.12.0",
    "next": "16.1.3",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "recharts": "^3.7.0"
  },

  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "eslint": "^9.39.2",
    "eslint-config-next": "^16.1.3",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0"
  }
}